<Window
    x:Class="Stacks.FanView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:Stacks"
    mc:Ignorable="d">

    <!-- Efek Mica/Acrylic bawaan WinUI 3 -->
    <Window.SystemBackdrop>
        <MicaBackdrop Kind="BaseAlt"/>
    </Window.SystemBackdrop>

    <Border Background="Transparent" 
            Padding="8">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <ItemsView x:Name="FileItemsControl" Grid.Row="0">
                <ItemsView.Layout>
                    <UniformGridLayout MinItemWidth="80" MinColumnSpacing="8" MinRowSpacing="8" />
                </ItemsView.Layout>
                <ItemsView.ItemTemplate>
                    <DataTemplate x:DataType="local:FileItem">
                        <StackPanel HorizontalAlignment="Center" Padding="8" Tapped="FileItem_Tapped">
                            <!-- Anda mungkin perlu menyesuaikan cara thumbnail ditampilkan di WinUI -->
                            <Image Source="{Binding Thumbnail}" Width="56" Height="56" Stretch="UniformToFill"/>
                            <TextBlock Text="{Binding FileName}" TextTrimming="CharacterEllipsis" TextAlignment="Center" MaxWidth="70" Margin="0,6,0,0" FontSize="11" TextWrapping="Wrap" MaxHeight="30"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsView.ItemTemplate>
            </ItemsView>

            <StackPanel Grid.Row="1" Margin="0,4,0,0">
                <Rectangle Height="1" Fill="{ThemeResource CardStrokeColorDefaultBrush}" Margin="8,0,8,8" Opacity="0.5"/>
                <Button x:Name="OpenInExplorerButton" Content="Open in File Explorer" HorizontalAlignment="Stretch" Margin="8,0,8,4"/>
                <Button x:Name="CloseButton" Content="Close" HorizontalAlignment="Stretch" Margin="8,4,8,4" Click="CloseButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>